{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "local": "cp -r customauth/* node_modules/ && NODE_OPTIONS=\"--trace-warnings\" npx directus start",
    "staging": "cp -r customauth/* node_modules/ && npx directus start",
    "production": "cp -r customauth/* node_modules/ npx directus start"
  },
  "type": "module",
  "dependencies": {
    "jsonwebtoken": "^9.0.1",
    "pg": "^8.11.2"
  }
}
